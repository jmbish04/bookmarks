# Frontend (Shadcn Dark Theme)

This React + Vite frontend uses the default shadcn/ui dark theme tokens and is served as Worker assets.

## Commands
- `pnpm dev` – start the Vite dev server
- `pnpm build` – build to `../dist` for Worker assets
